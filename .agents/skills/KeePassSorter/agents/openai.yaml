interface:
  display_name: "KeePassSorter"
  short_description: "Repo-specific patterns and workflows for KeePassSorter"
  default_prompt: "Use the KeePassSorter repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true